require(['jquery','YSDForms'], function($, YSDForms) {

  $(document).ready(function() {
    $('#backend_front_page').bind('load', function() {
      var cssLink = document.createElement("link") 
      cssLink.href = "/dashboard/css/styles.css"; 
      cssLink.rel = "stylesheet"; 
      cssLink.type = "text/css"; 
      frames['backend_front_page'].contentDocument.body.appendChild(cssLink);
      $('#backend_front_page').show();
    });
    $('#backend_front_page').attr('src', $('#site\\.front_page').val());
  });

});